{% extends 'base.html.twig' %}

{% block title %}Contacts{% endblock %}

{% block body %}
    <h1>Contacts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.nom }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ contact.fonction }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ contact.telephone }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ contact.avatar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('controller_contacts_index') }}">back to list</a>

    <a href="{{ path('controller_contacts_edit', {'id': contact.id}) }}">edit</a>

    {{ include('controller_contacts/_delete_form.html.twig') }}
{% endblock %}
