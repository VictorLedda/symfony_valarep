{% extends 'base.html.twig' %}

{% block title %}Edit Contacts{% endblock %}

{% block body %}
    <h1>Edit Contacts</h1>

    {{ include('controller_contacts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('controller_contacts_index') }}">back to list</a>

    {{ include('controller_contacts/_delete_form.html.twig') }}
{% endblock %}
