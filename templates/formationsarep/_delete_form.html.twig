<form method="post" action="{{ path('formationsarep_delete', {'id': formationsarep.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formationsarep.id) }}">
    <button class="btn">Delete</button>
</form>
