{% extends 'base.html.twig' %}

{% block title %}Formationsarep index{% endblock %}

{% block body %}
    <h1>Formationsarep index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Genre</th>
                <th>Documentation</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formationsarep in formationsareps %}
            <tr>
                <td>{{ formationsarep.id }}</td>
                <td>{{ formationsarep.titre }}</td>
                <td>{{ formationsarep.genre }}</td>
                <td>{{ formationsarep.documentation }}</td>
                <td>{{ formationsarep.image }}</td>
                <td>
                    <a href="{{ path('formationsarep_show', {'id': formationsarep.id}) }}">show</a>
                    <a href="{{ path('formationsarep_edit', {'id': formationsarep.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('formationsarep_new') }}">Create new</a>
{% endblock %}
